package:
  name: ccfits
  version: 2.5

build:
  number: 0

requirements:
  build:
    - {{ compiler('cxx') }}
    - {{ compiler('c') }}
    - {{ compiler('fortran') }}
    - cfitsio

  host:
    - cfitsio

  run:
    - cfitsio

about:
  home: http://heasarc.gsfc.nasa.gov/docs/software/fitsio/ccfits/

